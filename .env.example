# Copy this file to .env and fill in values before running docker compose.
# .env is gitignored — never commit real secrets.

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------

# MongoDB database name (default: treasure)
MONGODB_DATABASE=treasure

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------

# Port the frontend container exposes on the host (default: 8080)
APP_PORT=8080

# ---------------------------------------------------------------------------
# LLM provider (Phase 2 — leave blank for now)
# ---------------------------------------------------------------------------

# Provider: openai | anthropic | ollama
# LLM_PROVIDER=anthropic

# OpenAI
# OPENAI_API_KEY=

# Anthropic
# ANTHROPIC_API_KEY=

# Ollama (local)
# OLLAMA_BASE_URL=http://localhost:11434

# ---------------------------------------------------------------------------
# Gmail integration (Phase 2 — leave blank for now)
# ---------------------------------------------------------------------------

# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=

# ---------------------------------------------------------------------------
# Security (Phase 1, Story 1.3/1.4 — leave blank for now)
# ---------------------------------------------------------------------------

# Fernet encryption key for sensitive fields (generated on first run if unset)
# ENCRYPTION_KEY=

# Session secret for web UI auth
# SESSION_SECRET=
